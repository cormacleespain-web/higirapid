# Decisions

Significant product/technical decisions with date, agent, and rationale.

| Date       | Agent            | Decision | Rationale |
|------------|------------------|----------|-----------|
| 2026-03-14 | team-orchestrator | Use only `next/font/google` for Google Fonts; no `<link>` or external stylesheet to fonts.googleapis.com | Single source of truth and better performance: next/font self-hosts at build time, avoids extra network requests and layout shift; duplicate loading would be redundant and harder to maintain. Documented in `docs/fonts.md` and `src/lib/fonts.ts`. |
| 2026-03-14 | team-orchestrator | Centralize all font loading in `src/lib/fonts.ts`; layout applies CSS variables; Tailwind consumes them | One place to add or change fonts; consistent pattern for Google (next/font/google) and local (next/font/local) fonts; avoids scattered font config. |
| 2026-03-14 | frontend-engineer | Hero full-bleed background: Next Image with fill + object-cover in absolute inset-0 container; image source `https://picsum.photos/1920/1080?random=hero` | Single high-resolution asset for cover quality; no new deps; final hero asset can be swapped via constant or public/ path later. |
| 2026-03-14 | frontend-engineer | Hero overlay: `bg-black/60` (60% opacity) for dark overlay on background image | Ensures WCAG 2.1 AA contrast for white/light text on overlay; 50% was borderline, 60% gives safe contrast. |
| 2026-03-14 | frontend-engineer | Hero text: headline `text-content-inverse`, subhead `text-white/90`; buttons unchanged (primary and WhatsApp already high contrast) | Reuses design token for inverse text; slight tint on subhead for hierarchy; primary and WhatsApp variants already meet contrast on dark. |
| 2026-03-14 | frontend-engineer | Hero focus: `focus-visible:ring-offset-black/80` on Hero CTAs so focus ring is visible on dark overlay | Default ring-offset is light; on dark overlay a dark ring-offset keeps the focus ring clearly visible for keyboard users. |
| 2026-03-14 | frontend-engineer | Hero layout: centered single column; right-side inline image removed; content wrapper flex + items-center + justify-center + text-center | Design requirement for full-bleed bg and centered copy; hero image is now the section background so inline image removed. |
| 2026-03-14 | frontend-artist | Testimonials section background: `.bg-testimonials` utility with gradient (primary → subtle secondary hint) + spotted pattern for depth and theme fit; no animation; tokens only | Plan: layered depth (gradient + pattern) supports trust/calm and abstract freshness; reuses design tokens and existing spotted pattern; static so no reduced-motion concerns. |
| 2026-03-24 | Cursor agent | Add Codex skill `admin-planner` at `~/.codex/skills/admin-planner` (generic, not HigiRapid-specific) | Reusable planning skill for admin IA, CMS-style content, products/services, contact CTAs, env vs DB auth, and scaling patterns; keeps project repo free of skill boilerplate while satisfying workspace decision-logging for significant deliverables. |
| 2026-03-24 | Cursor agent | HigiRapid admin CMS: Neon + Drizzle, Auth.js single admin (env email + bcrypt hash), `/admin` outside `[locale]`, `content_entries` overrides + DB services/gallery/site_settings, Vercel Blob upload API, `revalidatePath` per locale on mutations | Matches Vercel + single-admin plan; Edge-safe auth split (`auth.config.ts` vs credentials provider); public site falls back to JSON/hardcoded data when `DATABASE_URL` is unset so local builds keep working. |
| 2026-03-24 | Cursor agent | Support optional `ADMIN_PASSWORD_HASH_B64` (preferred) plus hardened hash normalization; `AUTH_DEBUG_LOGIN=1` logs non-secret hash metadata before `bcrypt.compare` | Base64 avoids `$` being interpreted by shell/dotenv expansion; normalization strips newlines/whitespace from pasted hashes; debug helps distinguish email mismatch vs wrong password without logging secrets. |
| 2026-03-24 | Cursor agent | Admin UX: page copy shows JSON defaults beside overrides, section tabs + per-section save, essential vs full sections via `editorTier` in `content-admin-keys.ts`; shared `AdminImageField` for hero + gallery; gallery list thumbnails; services split customer/advanced + icon radios; delete confirmations | Aligns with admin-planner (structured fields, alt text, Blob uploads, settings centralization) while reducing jargon and technical keys for non-technical editors; auth and server validation unchanged. |
| 2026-03-24 | Cursor agent | Admin shell uses public-site Tailwind tokens + Garet/Nunito variables; client `AdminSidebar` with active route + “View website” to `/${defaultLocale}`; optional DeepL (`DEEPL_API_KEY`) for “Suggest translations” on page copy sections and service titles/descriptions (IP rate limit, server-only, review before save) | Brand alignment without duplicating marketing layout; translation is assistive only—editors must save; copy sent to third-party API documented in `.env.example` and UI. |
| 2026-03-26 | Cursor agent | Added `blog_posts` + `blog_post_i18n` and `hr_club_leads` models; extended `site_settings` with `hrClubRecipientEmail` | Supports admin-managed multilingual blog publishing, HR-Club lead inbox workflows, and configurable routing for lead notifications without code edits. |
| 2026-03-26 | Cursor agent | Introduced new localized public routes (`/hr-club`, `/blog`, `/blog/[slug]`, `/facade-cleaning`) plus homepage `HR-Club` teaser and `Why choose us` section | Delivers requested IA/content expansion while keeping existing homepage structure intact and minimizing regression risk. |
| 2026-03-26 | Cursor agent | Added localized fallback SEO blog set (3 posts per locale) in `src/lib/blog-fallback.ts` | Ensures blog launch readiness even when DB content has not yet been seeded or is temporarily unavailable. |
