# Decisions

Significant product/technical decisions with date, agent, and rationale.

| Date       | Agent            | Decision | Rationale |
|------------|------------------|----------|-----------|
| 2026-03-14 | team-orchestrator | Use only `next/font/google` for Google Fonts; no `<link>` or external stylesheet to fonts.googleapis.com | Single source of truth and better performance: next/font self-hosts at build time, avoids extra network requests and layout shift; duplicate loading would be redundant and harder to maintain. Documented in `docs/fonts.md` and `src/lib/fonts.ts`. |
| 2026-03-14 | team-orchestrator | Centralize all font loading in `src/lib/fonts.ts`; layout applies CSS variables; Tailwind consumes them | One place to add or change fonts; consistent pattern for Google (next/font/google) and local (next/font/local) fonts; avoids scattered font config. |
| 2026-03-14 | frontend-engineer | Hero full-bleed background: Next Image with fill + object-cover in absolute inset-0 container; image source `https://picsum.photos/1920/1080?random=hero` | Single high-resolution asset for cover quality; no new deps; final hero asset can be swapped via constant or public/ path later. |
| 2026-03-14 | frontend-engineer | Hero overlay: `bg-black/60` (60% opacity) for dark overlay on background image | Ensures WCAG 2.1 AA contrast for white/light text on overlay; 50% was borderline, 60% gives safe contrast. |
| 2026-03-14 | frontend-engineer | Hero text: headline `text-content-inverse`, subhead `text-white/90`; buttons unchanged (primary and WhatsApp already high contrast) | Reuses design token for inverse text; slight tint on subhead for hierarchy; primary and WhatsApp variants already meet contrast on dark. |
| 2026-03-14 | frontend-engineer | Hero focus: `focus-visible:ring-offset-black/80` on Hero CTAs so focus ring is visible on dark overlay | Default ring-offset is light; on dark overlay a dark ring-offset keeps the focus ring clearly visible for keyboard users. |
| 2026-03-14 | frontend-engineer | Hero layout: centered single column; right-side inline image removed; content wrapper flex + items-center + justify-center + text-center | Design requirement for full-bleed bg and centered copy; hero image is now the section background so inline image removed. |
| 2026-03-14 | frontend-artist | Testimonials section background: `.bg-testimonials` utility with gradient (primary → subtle secondary hint) + spotted pattern for depth and theme fit; no animation; tokens only | Plan: layered depth (gradient + pattern) supports trust/calm and abstract freshness; reuses design tokens and existing spotted pattern; static so no reduced-motion concerns. |
| 2026-03-24 | Cursor agent | Add Codex skill `admin-planner` at `~/.codex/skills/admin-planner` (generic, not HigiRapid-specific) | Reusable planning skill for admin IA, CMS-style content, products/services, contact CTAs, env vs DB auth, and scaling patterns; keeps project repo free of skill boilerplate while satisfying workspace decision-logging for significant deliverables. |
| 2026-03-24 | Cursor agent | HigiRapid admin CMS: Neon + Drizzle, Auth.js single admin (env email + bcrypt hash), `/admin` outside `[locale]`, `content_entries` overrides + DB services/gallery/site_settings, Vercel Blob upload API, `revalidatePath` per locale on mutations | Matches Vercel + single-admin plan; Edge-safe auth split (`auth.config.ts` vs credentials provider); public site falls back to JSON/hardcoded data when `DATABASE_URL` is unset so local builds keep working. |
| 2026-03-24 | Cursor agent | Support optional `ADMIN_PASSWORD_HASH_B64` (preferred) plus hardened hash normalization; `AUTH_DEBUG_LOGIN=1` logs non-secret hash metadata before `bcrypt.compare` | Base64 avoids `$` being interpreted by shell/dotenv expansion; normalization strips newlines/whitespace from pasted hashes; debug helps distinguish email mismatch vs wrong password without logging secrets. |
| 2026-03-24 | Cursor agent | Admin UX: page copy shows JSON defaults beside overrides, section tabs + per-section save, essential vs full sections via `editorTier` in `content-admin-keys.ts`; shared `AdminImageField` for hero + gallery; gallery list thumbnails; services split customer/advanced + icon radios; delete confirmations | Aligns with admin-planner (structured fields, alt text, Blob uploads, settings centralization) while reducing jargon and technical keys for non-technical editors; auth and server validation unchanged. |
| 2026-03-24 | Cursor agent | Admin shell uses public-site Tailwind tokens + Garet/Nunito variables; client `AdminSidebar` with active route + “View website” to `/${defaultLocale}`; optional DeepL (`DEEPL_API_KEY`) for “Suggest translations” on page copy sections and service titles/descriptions (IP rate limit, server-only, review before save) | Brand alignment without duplicating marketing layout; translation is assistive only—editors must save; copy sent to third-party API documented in `.env.example` and UI. |
| 2026-03-26 | Cursor agent | Added `blog_posts` + `blog_post_i18n` and `hr_club_leads` models; extended `site_settings` with `hrClubRecipientEmail` | Supports admin-managed multilingual blog publishing, HR-Club lead inbox workflows, and configurable routing for lead notifications without code edits. |
| 2026-03-26 | Cursor agent | Introduced new localized public routes (`/hr-club`, `/blog`, `/blog/[slug]`, `/facade-cleaning`) plus homepage `HR-Club` teaser and `Why choose us` section | Delivers requested IA/content expansion while keeping existing homepage structure intact and minimizing regression risk. |
| 2026-03-26 | Cursor agent | Added localized fallback SEO blog set (3 posts per locale) in `src/lib/blog-fallback.ts` | Ensures blog launch readiness even when DB content has not yet been seeded or is temporarily unavailable. |
| 2026-03-26 | Cursor agent | Implemented App Router `sitemap.xml` and `robots.txt` with canonical base URL `https://higirapid.es`, locale-prefixed coverage, and admin/API exclusions | Aligns crawl/index signals for Google Search, prevents accidental indexing of private routes, and provides a stable sitemap entry point for Search Console submission. |
| 2026-03-26 | Cursor agent | Added canonical metadata on localized blog index pages to consolidate query-filter variants to `/{locale}/blog` | Prevents duplicate-content dilution from `?category=` URLs while preserving crawlability and clean canonical signals for Google. |
| 2026-03-30 | Cursor agent | FAQ CMS: `faq.order` JSON (slot ids 1–20) synced across locales + `faq.qN`/`faq.aN` keys; admin `FaqItemsAdmin` drag-reorder (same pattern as services), add/delete, per-slot locale edit; public `FAQ.tsx` reads order and hides empty pairs | Keeps existing `content_entries` model and translation flow; avoids a separate FAQ table; slot cap 20 is enough for marketing use; DeepL skips `faq.order` so JSON is not machine-translated. |
| 2026-04-01 | Cursor agent | Services: extend `service_offerings` with `image_url`, `image_object_position`, `price_from`; `service_offering_i18n` with `image_alt`; homepage carousel (Embla) + `/[locale]/services` page; `getPublishedServicesList` falls back to locale JSON when DB empty | Admin controls copy, image, icon, languages, from price, and sort order for both surfaces; alt required when an image URL is set. |
| 2026-04-01 | Cursor agent | Extend `POST /api/hr-club-leads` with optional `serviceSlug`/`serviceTitle`; persist `hr_club_leads.service_slug`; email subject `Service inquiry: {title} — {name}` when service context present; shared `LeadInquiryForm` + `ServiceBookingOverlay` (bottom sheet on narrow, centered dialog on `sm+`) | Reuses one pipeline and inbox; avoids duplicate lead endpoints; `inquiryType` fixed to `home` for service bookings to satisfy existing validation. |
| 2026-04-01 | Cursor agent | Services page **Contact** link: `mailto:contactEmail` with translated subject when `site_settings.contact_email` set; else WhatsApp `getContactHref` | Matches plan preference for email when configured; consistent with footer behaviour when email is absent. |
| 2026-04-01 | Cursor agent | Lead notifications: validate Resend HTTP response; log failures; return `{ emailSent, persisted }`; `reply_to` (snake_case) for Resend REST; 503 if lead is neither saved nor emailed; UI note when saved without email | Removes silent “success” when email never sends; surfaces misconfiguration (missing `RESEND_*`, recipient, or domain verification) via logs and honest copy. |
| 2026-04-01 | Cursor agent | `next.config.ts`: `serverExternalPackages: ["drizzle-orm", "@neondatabase/serverless"]` | Fixes intermittent dev/runtime `Cannot find module './vendor-chunks/drizzle-orm.js'` when webpack splits DB deps incorrectly; load ORM/Neon from `node_modules` on the server instead. |
| 2026-04-01 | Cursor agent | `ServiceIconGlyph`: named lucide imports + `ICON_BY_NAME` map (no `import * from "lucide-react"`) | Fixes `Cannot find module './vendor-chunks/lucide-react.js'` on `/services`; `lucide-react` cannot be in `serverExternalPackages` because Next merges it into `transpilePackages` via `optimizePackageImports`. |
| 2026-04-01 | Cursor agent | HR-Club lead recipient: `fromDb || env` after trim (not `??`) so empty `site_settings.hr_club_recipient_email` does not override env; log Resend `to` + top-level `id`; JSON may include `resendEmailId` | Empty string in DB made `to` empty, so Resend was skipped while the lead still saved — UI looked “successful” with no inbox mail. |
| 2026-04-01 | Cursor agent | Services: `price_was` on `service_offerings`; sale UI when `priceWas > priceFrom`; `getServicePlaceholderImage` + object position when `image_url` empty; nav label “Our Services” (en/es/ca); shared `ServicePriceImageBadge` / `ServicePriceInline` | Restores marketing placeholders for known slugs; admin-controlled strikethrough “was” + current price + red Sale label on carousel and `/services`. |
| 2026-04-01 | Cursor agent | Homepage services carousel: `getServicePlaceholderImage(slug, iconKey)` always resolves an asset; card footer `mt-auto` + `justify-between` (Book / Learn more); full-bleed track (`w-screen` + translate) with gutter-aligned first slide; narrower slide flex-basis for more visible cards | Matches services carousel refresh plan; unknown slugs no longer show empty image gradient when DB image is unset. |
| 2026-04-01 | Cursor agent | Services carousel polish: description in `min-w-0` + `overflow-hidden` wrapper with `line-clamp-3` + `break-words` (no `flex-1`/`min-h` on `<p>`); track gutter `pl-3/4` only (no lg align to 72rem); arrows centered below track; “View all” card gradient, `LayoutGrid` icon, hint line + pill CTA; `services.viewAllHint` i18n | Fixes clipped copy without ellipsis; edge-aligned strip on large screens; clearer hierarchy and motion-safe hover on final slide. |
| 2026-04-01 | Cursor agent | Sale strikethrough copy: `services.wasPrice` (“Was €{price}” / ES “Antes {price} €” / CA “Abans {price} €”) instead of repeating `fromPrice` on the old amount; `ServicePriceImageBadge` only on homepage carousel — removed from `/services` full page (pricing via `ServicePriceInline` only) | Clearer “was” semantics; avoids duplicate floating price on large service detail images while keeping carousel at-a-glance pricing. |
| 2026-04-01 | Cursor agent | Gallery grid cards (`Gallery.tsx`): same shell as services carousel — `rounded-xl` `article`, `aspect-[4/3]` image on top, caption + `fromPrice` in `p-4 sm:p-5` below (no in-image caption overlay); `unoptimized` for `http` image URLs | Visual parity with homepage service cards; clearer hierarchy and less busy thumbnails. |
| 2026-04-01 | Cursor agent | No `next/font` imports from `"use client"` sections: headings use Tailwind `font-heading` (CSS vars from locale/admin layouts only) | Fixes intermittent `Cannot find module './vendor-chunks/next.js'` on `[locale]/page` and restores fonts when the app no longer crashes before layout variables apply. |
| 2026-04-02 | Cursor agent | Lead emails via Resend **templates**: `higirapid-enquiry-template` when `serviceSlug`+`serviceTitle` present, else `hr-club-enquiry`; `template.variables` use `SUBMITTER_NAME`, `SUBMITTER_EMAIL`, `ENQUIRY_TYPE`, `SOURCE_URL` (`NEXT_PUBLIC_BASE_URL` + path), `SUBMITTED_AT` (ISO at send), `PHONE`, `LOCALE`, `MESSAGE`, `SERVICE_SLUG`, `SERVICE_TITLE`; optional `RESEND_TEMPLATE_*` overrides | Matches published Resend `{{{…}}}`; avoids reserved `EMAIL`; subject/reply_to still from API. |
